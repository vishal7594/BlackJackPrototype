
public class Global
{
    public enum GameState
    {
        Idle,
        Card_Display_Timer,
        Global_Turn_Timer,
        Winning
    }

    public enum PlayerState
    {
        Playing,
        Stand
    }
}